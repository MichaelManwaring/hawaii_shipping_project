<% Job.all.each do |j| %>
   <% if @user = current_user %>
   	  <%= j.job_name %> |
      <%= link_to 'Delete',  {:controller => "ships", :action => 'destroy', :id => j.id}, :confirm => "Are you sure you want to delete this ship?", :method => :delete %> <br>
   <% end %>
<% end %>
